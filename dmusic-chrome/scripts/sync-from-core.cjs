/**
 * 已弃用：整树复制由 npm run build（Vite）替代。
 * 保留本脚本以便旧文档或习惯命令仍打印迁移说明。
 *
 * @author dmusic-chrome
 */
// eslint-disable-next-line no-console
console.warn(
  '[dmusic-chrome] sync-from-core 已弃用。请在 dmusic-chrome 目录执行：npm run build\n' +
    '然后在 Chrome 中「加载已解压的扩展程序」选择本包的 dist/ 目录。'
);
process.exit(0);
