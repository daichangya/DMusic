{
  "name": "dmusic-chrome",
  "version": "0.2.0",
  "private": true,
  "description": "DMusic Chrome MV3 扩展（从 dmusic-core 同步构建 dist）",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.cjs",
    "verify:manifest": "node scripts/verify-manifest.mjs",
    "test": "npm run verify:manifest",
    "postinstall": "npm run build"
  }
}
